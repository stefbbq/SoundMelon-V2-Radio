.logo
	/= image_tag('core/footer_logo.png')
.right
	.menu-wrapper= render 'layouts/footer_menu'
		
	- if signed_in?
		= render "layouts/modal_boxes"
.clear

:javascript


	$('document').ready(function() {

		var fromCallback = '#{flash[:music_account_callback].to_s}';
		if(fromCallback === 'true') {
			$('.add-content').click();
		}

	});
